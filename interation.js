count = 0;
    for (rep = 5; rep <= 120; rep++) {
        counter = numCounter(6);
        if (counter == 10) {
            count++;        // uses increment operator (see below)
        }
    }

  console.log('count',count);
  
  
